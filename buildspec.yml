version: 0.3
phases:
 build:
  commands: 
    - echo Enter the build phase
    - mvn clean package
 post_build:
   commands:
    - echo build completed successfully
artifacts: 
 files: 
  - target/webapp.jar
 discard-paths: yes
